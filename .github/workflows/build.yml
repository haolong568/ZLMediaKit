name: Build All Platforms

on:
  push:
    branches: ['**']
    tags-ignore: ['**']
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  # ============================================
  # Job 0: 取消同一次 push 触发的其他所有 workflow
  # ============================================
  cancel-others:
    runs-on: ubuntu-latest
    steps:
      - name: 取消其他 workflow runs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "当前 workflow run ID: ${{ github.run_id }}"
          echo "当前 commit SHA: ${{ github.sha }}"
          echo ""
          echo "正在查找同一 commit 触发的其他 workflow runs..."

          runs=$(gh api \
            "repos/${{ github.repository }}/actions/runs?head_sha=${{ github.sha }}&per_page=100" \
            --jq '.workflow_runs[] | select(.id != ${{ github.run_id }}) | "\(.id) \(.name) \(.status)"')

          if [ -z "$runs" ]; then
            echo "✅ 没有其他 workflow runs 需要取消"
          else
            echo "发现以下 workflow runs 将被取消:"
            echo "$runs"
            echo ""

            echo "$runs" | while read -r run_id run_name run_status; do
              if [ "$run_status" = "queued" ] || [ "$run_status" = "in_progress" ]; then
                echo "🚫 取消: $run_name (ID: $run_id, 状态: $run_status)"
                gh api \
                  "repos/${{ github.repository }}/actions/runs/$run_id/cancel" \
                  --method POST || true
              fi
            done

            echo ""
            echo "✅ 已取消所有其他 workflow runs"
          fi

  # ============================================
  # Job 1: 矩阵构建 Windows + Linux（并行执行）
  # ============================================
  build:
    needs: cancel-others
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-2022
            platform: windows
            artifact_name: ZLMediaKit-Windows-x64.zip
            vcpkg_triplet: x64-windows-static
            bootstrap: bootstrap-vcpkg.bat

          - os: ubuntu-22.04
            platform: linux
            artifact_name: ZLMediaKit-Linux-x64.tar.gz
            vcpkg_triplet: x64-linux
            bootstrap: bootstrap-vcpkg.sh

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: 79d795a767da85bbba821b871265fd87d853d808
          submodules: recursive
          fetch-depth: 0

      - name: 记录实际编译的 commit
        run: echo "BUILT_SHA=$(git rev-parse HEAD)" >> $GITHUB_ENV
        shell: bash

      - name: 安装系统依赖 (Linux)
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake git pkg-config libssl-dev libsctp-dev

      # ------------------------------------------
      # vcpkg 安装（Windows 用 pwsh，Linux 用 bash）
      # ------------------------------------------
      - name: 安装 vcpkg 并编译依赖 (Windows)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          $vcpkgDir = Join-Path $env:GITHUB_WORKSPACE "vcpkg"
          git clone https://github.com/microsoft/vcpkg.git $vcpkgDir
          Set-Location $vcpkgDir
          git checkout efcfaaf60d7ec57a159fc3110403d939bfb69729
          .\bootstrap-vcpkg.bat
          .\vcpkg.exe install openssl libsrtp[openssl] usrsctp --triplet ${{ matrix.vcpkg_triplet }}

      - name: 安装 vcpkg 并编译依赖 (Linux)
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          VCPKG_DIR="${GITHUB_WORKSPACE}/vcpkg"
          git clone https://github.com/microsoft/vcpkg.git "$VCPKG_DIR"
          cd "$VCPKG_DIR"
          git checkout efcfaaf60d7ec57a159fc3110403d939bfb69729
          ./bootstrap-vcpkg.sh
          ./vcpkg install openssl libsrtp[openssl] usrsctp --triplet ${{ matrix.vcpkg_triplet }}

      # ------------------------------------------
      # CMake 配置
      # ------------------------------------------
      - name: CMake 配置（全功能开启）
        shell: bash
        run: |
          VCPKG_ROOT=$(echo "${{ github.workspace }}" | sed 's|\\|/|g')
          cmake -B build -S . \
            -DCMAKE_TOOLCHAIN_FILE="${VCPKG_ROOT}/vcpkg/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=${{ matrix.vcpkg_triplet }} \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_WEBRTC=ON \
            -DENABLE_SRT=ON \
            -DENABLE_RTPPROXY=ON \
            -DENABLE_HLS=ON \
            -DENABLE_MP4=ON \
            -DENABLE_API=ON \
            -DENABLE_TESTS=OFF \
            -DENABLE_FAAC=OFF \
            -DENABLE_X264=OFF \
            -DENABLE_FFMPEG=OFF

      # ------------------------------------------
      # 编译
      # ------------------------------------------
      - name: 编译 MediaServer (Windows)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          if (-not (Test-Path build/server/MediaServer.vcxproj)) {
            Get-ChildItem -Path build -Recurse -Filter *.vcxproj | Select-Object FullName
            throw "未找到 build/server/MediaServer.vcxproj"
          }
          cmake --build build --config Release --target MediaServer --parallel 4

      - name: 编译 MediaServer (Linux)
        if: matrix.platform == 'linux'
        run: |
          cmake --build build --config Release --target MediaServer --parallel $(nproc)

      # ------------------------------------------
      # 收集产物 (Windows)
      # ------------------------------------------
      - name: 收集产物 (Windows)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          $ws = $env:GITHUB_WORKSPACE
          New-Item -ItemType Directory -Force -Path dist | Out-Null

          Write-Host "=== 搜索 MediaServer.exe ==="
          $exe = Get-ChildItem -Path $ws -Recurse -Filter "MediaServer.exe" -ErrorAction SilentlyContinue |
                 Where-Object { $_.FullName -notmatch '\\vcpkg\\|\\\.git\\' }
          if ($exe) {
            foreach ($f in $exe) {
              Write-Host "找到: $($f.FullName) ($([math]::Round($f.Length/1MB,2)) MB)"
            }
            Copy-Item $exe[0].FullName -Destination dist/MediaServer.exe -Force
            Write-Host "✅ 已收集 MediaServer.exe"
          } else {
            Write-Host "❌ 未找到 MediaServer.exe，列出所有 .exe 文件:"
            Get-ChildItem -Path $ws -Recurse -Filter "*.exe" -ErrorAction SilentlyContinue |
              Where-Object { $_.FullName -notmatch '\\vcpkg\\|\\\.git\\' } |
              Select-Object FullName, @{N='Size(MB)';E={[math]::Round($_.Length/1MB,2)}}
            throw "未找到 MediaServer.exe"
          }

          # 收集配置文件
          @("server/config.ini", "conf/config.ini", "config.ini") | ForEach-Object {
            if (Test-Path $_) { Copy-Item $_ -Destination dist/config.ini -Force; Write-Host "收集配置: $_" }
          }

          # 收集证书
          $pem = Get-ChildItem -Path $ws -Recurse -Filter "default.pem" -ErrorAction SilentlyContinue |
                 Where-Object { $_.FullName -notmatch '\\vcpkg\\|\\\.git\\' } | Select-Object -First 1
          if ($pem) {
            Copy-Item $pem.FullName -Destination dist/default.pem -Force
            Write-Host "收集证书: $($pem.FullName)"
          } else {
            Write-Host "仓库未自带 default.pem，尝试生成自签名证书..."
            $opensslExe = (Get-Command openssl -ErrorAction SilentlyContinue).Source
            if (-not $opensslExe) {
              $opensslExe = Get-ChildItem -Path "$ws/vcpkg/installed" -Recurse -Filter "openssl.exe" -ErrorAction SilentlyContinue |
                            Select-Object -First 1 -ExpandProperty FullName
            }
            if ($opensslExe) {
              & $opensslExe req -x509 -newkey rsa:2048 -keyout "$ws/key.pem" -out "$ws/cert.pem" -days 3650 -nodes -subj "/CN=localhost"
              Get-Content "$ws/cert.pem", "$ws/key.pem" | Set-Content dist/default.pem
              Write-Host "已生成自签名 default.pem"
            } else { Write-Host "⚠ 未找到 openssl 工具，跳过证书生成" }
          }

          # 收集网页目录
          $www = Get-ChildItem -Path $ws -Recurse -Directory -ErrorAction SilentlyContinue |
                 Where-Object { $_.Name -in @('www','web') -and $_.FullName -notmatch '\\vcpkg\\|\\build\\|\\\.git\\' } |
                 Select-Object -First 1
          if ($www) {
            Copy-Item $www.FullName -Destination dist/www -Recurse -Force
            Write-Host "收集网页目录: $($www.FullName)"
          } else { Write-Host "⚠ 仓库中未发现 www/web 目录" }

          Write-Host "=== dist 最终内容 ==="
          Get-ChildItem dist -Recurse | Select-Object FullName, @{N='Size(MB)';E={[math]::Round($_.Length/1MB,2)}}

      # ------------------------------------------
      # 收集产物 (Linux)
      # ------------------------------------------
      - name: 收集产物 (Linux)
        if: matrix.platform == 'linux'
        run: |
          ws=$GITHUB_WORKSPACE
          mkdir -p dist

          echo "=== 搜索 MediaServer 可执行文件 ==="
          exe=""

          # 第一轮：按名称精确搜索
          for pattern in "MediaServer" "media_server" "zlmediakit"; do
            found=$(find "$ws" -name "$pattern" -type f -executable \
              -not -path "*/vcpkg/*" -not -path "*/.git/*" -not -path "*/CMakeFiles/*" 2>/dev/null | head -1)
            if [ -n "$found" ]; then
              exe="$found"
              break
            fi
          done

          # 第二轮：搜索 build 目录下大于 1MB 的可执行文件
          if [ -z "$exe" ]; then
            echo "按名称未找到，搜索 build 目录下所有大型可执行文件:"
            exe=$(find "$ws/build" -type f -executable -not -path "*/CMakeFiles/*" -size +1M 2>/dev/null | head -1)
          fi

          # 第三轮：检查 release 目录
          if [ -z "$exe" ]; then
            exe=$(find "$ws/release" -type f -executable -size +1M 2>/dev/null | head -1)
          fi

          if [ -n "$exe" ]; then
            echo "✅ 找到: $exe ($(du -h "$exe" | cut -f1))"
            cp "$exe" dist/MediaServer
            chmod +x dist/MediaServer
          else
            echo "❌ 未找到可执行文件"
            echo "=== build 目录结构 ==="
            find "$ws/build" -maxdepth 3 -type f 2>/dev/null | head -50
            echo ""
            echo "=== release 目录 ==="
            find "$ws/release" -maxdepth 3 -type f 2>/dev/null | head -50
            exit 1
          fi

          # 收集配置文件
          for cfg in "server/config.ini" "conf/config.ini" "config.ini"; do
            if [ -f "$cfg" ]; then
              cp "$cfg" dist/config.ini
              echo "收集配置: $cfg"
              break
            fi
          done

          # 收集证书
          pem=$(find "$ws" -name "default.pem" -not -path "*/vcpkg/*" -not -path "*/.git/*" 2>/dev/null | head -1)
          if [ -n "$pem" ]; then
            cp "$pem" dist/default.pem
            echo "收集证书: $pem"
          else
            echo "仓库未自带 default.pem，尝试生成自签名证书..."
            openssl_exe=$(which openssl 2>/dev/null)
            if [ -z "$openssl_exe" ]; then
              openssl_exe=$(find "$ws/vcpkg/installed" -name "openssl" -type f 2>/dev/null | head -1)
            fi
            if [ -n "$openssl_exe" ]; then
              $openssl_exe req -x509 -newkey rsa:2048 -keyout "$ws/key.pem" -out "$ws/cert.pem" -days 3650 -nodes -subj "/CN=localhost"
              cat "$ws/cert.pem" "$ws/key.pem" > dist/default.pem
              echo "已生成自签名 default.pem"
            else
              echo "⚠ 未找到 openssl 工具，跳过证书生成"
            fi
          fi

          # 收集网页目录
          www=$(find "$ws" -maxdepth 3 -type d \( -name "www" -o -name "web" \) -not -path "*/vcpkg/*" -not -path "*/build/*" -not -path "*/.git/*" 2>/dev/null | head -1)
          if [ -n "$www" ]; then
            cp -r "$www" dist/www
            echo "收集网页目录: $www"
          else
            echo "⚠ 仓库中未发现 www/web 目录"
          fi

          echo "=== dist 最终内容 ==="
          ls -lhR dist/

      # ------------------------------------------
      # 打包
      # ------------------------------------------
      - name: 打包 (Windows → zip)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path upload | Out-Null
          if (-not (Get-ChildItem -Path dist -Recurse -File -ErrorAction SilentlyContinue)) {
            Set-Content -Path dist/_EMPTY_BUILD_PLACEHOLDER.txt -Value "No binary artifacts."
          }
          Compress-Archive -Path dist\* -DestinationPath upload\ZLMediaKit-Windows-x64.zip
          Get-ChildItem upload | Select-Object Name, @{N='Size(MB)';E={[math]::Round($_.Length/1MB,2)}}

      - name: 打包 (Linux → tar.gz)
        if: matrix.platform == 'linux'
        run: |
          mkdir -p upload
          if [ ! "$(ls -A dist/ 2>/dev/null)" ]; then
            echo "No binary artifacts." > dist/_EMPTY_BUILD_PLACEHOLDER.txt
          fi
          tar -czvf upload/ZLMediaKit-Linux-x64.tar.gz -C dist .
          ls -lh upload/

      # ------------------------------------------
      # 上传产物
      # ------------------------------------------
      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.platform }}
          path: upload/*
          retention-days: 1

  # ============================================
  # Job 2: 统一发布 Release
  # ============================================
  release:
    needs: build
    runs-on: ubuntu-22.04
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads/')

    steps:
      # 🔧 必须 checkout，否则 gh 命令找不到 git 仓库
      - name: Checkout 代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 下载所有构建产物
        uses: actions/download-artifact@v4
        with:
          path: all-packages
          pattern: package-*
          merge-multiple: true

      - name: 查看下载的产物
        run: |
          echo "=== 所有待发布文件 ==="
          ls -lh all-packages/

      - name: 创建统一 Release 并上传所有 asset
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
          RUN_NUMBER: ${{ github.run_number }}
          REPO: ${{ github.repository }}
        run: |
          set -e
          tag="build-$RUN_NUMBER"
          title="ZLMediaKit 全平台构建 #$RUN_NUMBER"
          body="## 全平台自动构建

          | 平台 | 文件 |
          |------|------|
          | Windows x64 | ZLMediaKit-Windows-x64.zip |
          | Linux x64 | ZLMediaKit-Linux-x64.tar.gz |

          **对应 commit:** \`79d795a767da85bbba821b871265fd87d853d808\`
          **包内含:** MediaServer / config.ini / default.pem / www(如有)
          **功能:** WebRTC / SRT / HLS / RTSP / RTMP / HTTP-FLV / GB28181"

          echo "✨ 创建 Release: $tag ..."

          assets=""
          for f in all-packages/*; do
            if [ -f "$f" ]; then
              assets="$assets $f"
              echo "  📦 将上传: $(basename $f)"
            fi
          done

          gh release create "$tag" $assets \
            --repo "$REPO" \
            --title "$title" \
            --notes "$body" \
            --latest

          echo "✅ Release 发布成功: $tag"
